{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "rule_id": {
      "type": "string",
      "pattern": "^AB-[A-Z0-9-]+$",
      "description": "Stable rule identifier (e.g. 'AB-001', 'AB-007')"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semver of this rule's check logic — see spec §3"
    },
    "name": {
      "type": "string",
      "description": "Human-readable rule name"
    },
    "category": {
      "type": "string",
      "enum": [
        "discovery",
        "documentation",
        "actionability",
        "machine_readable",
        "verification",
        "content_negotiation",
        "payments",
        "bazaar",
        "openapi",
        "skills",
        "agents_txt",
        "webmcp",
        "identity",
        "bot_auth",
        "infrastructure",
        "seo_aeo",
        "accessibility",
        "active_probing",
        "pricing",
        "rate_limits",
        "error_semantics",
        "retry_semantics",
        "sandbox",
        "versioning",
        "agent_policy"
      ],
      "description": "Rule category (v0.4 §A.2): discovery, documentation, actionability, machine_readable, verification, content_negotiation, payments, bazaar, openapi, skills, agents_txt, webmcp, identity, bot_auth, infrastructure, seo_aeo, accessibility, active_probing, pricing, rate_limits, error_semantics, retry_semantics, sandbox, versioning, agent_policy"
    },
    "severity": {
      "type": "string",
      "enum": [
        "critical",
        "high",
        "medium",
        "low"
      ],
      "description": "Rule severity: critical (agents cannot proceed — triggers total cap ≤ 30, v0.4 §A.3/§6.3), high (triggers category floor), medium (normal), low (informational)"
    },
    "counted_in_score": {
      "type": "boolean",
      "description": "If false, this rule does not contribute to score calculation"
    },
    "applicability": {
      "description": "Optional applicability condition — if absent, rule always applies",
      "type": "object",
      "properties": {
        "condition": {
          "type": "string",
          "description": "Machine-readable condition expression"
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of when this rule applies"
        }
      },
      "required": [
        "condition",
        "description"
      ],
      "additionalProperties": false
    },
    "check": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "http_fetch",
            "schema_validation",
            "exact_match",
            "cross_evidence",
            "http_probe",
            "content_parse",
            "json_rpc",
            "header_check",
            "semantic_validation"
          ],
          "description": "Check type: http_fetch (HTTP GET + parse), schema_validation (validate against JSON Schema), exact_match (string/structural equality), cross_evidence (compare two fetched sources on shared key), http_probe (HTTP request with status/header check), content_parse (parse response body for specific content), json_rpc (JSON-RPC call to MCP endpoint), header_check (verify specific HTTP headers), semantic_validation (pure function parsing structured sources for semantic criteria — no LLM, deterministic boundary, v0.4 §A.4)"
        },
        "sources": {
          "description": "Array of source identifiers to fetch/compare (for http_fetch, cross_evidence)",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "match_keys": {
          "description": "Keys to match when type = exact_match or cross_evidence",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "conflict_when": {
          "description": "Condition expression describing when sources conflict (for cross_evidence)",
          "type": "string"
        },
        "target": {
          "description": "Target path or URL for http_fetch / schema_validation",
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "description": "Check definition — how the rule is evaluated"
    },
    "pillar": {
      "type": "string",
      "enum": [
        "discovery",
        "understandability",
        "executability",
        "verifiability"
      ],
      "description": "Optional pillar override — if absent, pillar is derived from category via CATEGORY_TO_PILLAR map (spec v0.2 §A.8)"
    },
    "fix": {
      "type": "object",
      "properties": {
        "eligible": {
          "type": "boolean",
          "description": "Whether AgentBadge can generate a fix for this rule"
        },
        "type": {
          "type": "string",
          "enum": [
            "deterministic",
            "assisted",
            "none"
          ],
          "description": "Fix type: deterministic (safe to auto-generate), assisted (requires human confirm/edit/reject), none (not fixable by AgentBadge)"
        },
        "note": {
          "description": "Additional context about the fix approach",
          "type": "string"
        }
      },
      "required": [
        "eligible",
        "type"
      ],
      "additionalProperties": false,
      "description": "Fix definition — how a failing rule can be remediated"
    }
  },
  "required": [
    "rule_id",
    "version",
    "name",
    "category",
    "severity",
    "counted_in_score",
    "check",
    "fix"
  ],
  "additionalProperties": false
}