# Common Prerequisites

Before following any guide in this documentation, ensure you have:

- **Node.js** >= 20
- **Bun** installed (`curl -fsSL https://bun.sh/install | bash`)
- **Git** configured with your GitHub account
- A Hedera testnet account (for blockchain features)
