export const COMPARISON_PAGES: Array<{ slug: string; title: string }> = [];
export function getComparisonPage(_slug: string): { slug: string; title: string; markdown: string } | null {
  return null;
}
